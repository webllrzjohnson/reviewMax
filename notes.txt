PROMPT and CLAUDE NOTE:

You are a senior full-stack engineer.

Build a complete, production-ready web app using:
- Next.js 14+ (App Router)
- TypeScript (strict mode)
- Supabase (Auth, Database, Storage, RLS)
- Tailwind CSS + shadcn/ui
- Zod for validation
- Deployed on Vercel

APP IDEA:
A product review website where AI-generated blog posts and product reviews 
are published automatically via an n8n automation pipeline. Readers browse 
reviews by category, compare products, and click through to Amazon via 
affiliate links. An admin submits a review request through a form, which 
triggers n8n to generate a full blog post using the Claude API, upload any 
images to Google Drive, store all content in Supabase, and publish it to 
the site automatically — no manual publishing required.

GOAL:
Generate a clean, scalable, beginner-friendly project that is fully 
functional. No placeholder logic. All code must be complete and immediately 
runnable.

---

OUTPUT FORMAT (STRICT):

1. PROJECT OVERVIEW
   - Brief description: A content-driven Amazon affiliate review site where 
     all blog posts and product reviews are generated by Claude AI and 
     published automatically via n8n. The admin never writes content manually.
   - Target users: Readers searching for product recommendations and buying 
     guides. Admin manages topic submissions only.
   - Core features (list only what will actually be built):
     · Public homepage with featured reviews and latest posts
     · Category pages (e.g., Kitchen, Tech, Home, Fitness, Beauty)
     · Individual product review pages with pros/cons, star rating, 
       verdict, and Amazon affiliate buy button
     · Blog index page with search and category filter
     · Individual blog post pages with SEO metadata
     · Admin dashboard (protected, role: admin) with a review request form
     · n8n webhook endpoint that receives form submission and triggers 
       AI content generation
     · Claude API integration in n8n generates full review post 
       (title, excerpt, body, pros, cons, rating, verdict, 
       Amazon affiliate link placeholder)
     · Google Drive integration in n8n for product image storage
     · Supabase as the database and content store — n8n writes directly 
       via Supabase REST API
     · Auto-publish on save — no manual approval step needed
     · Related posts widget on each review page
     · Newsletter signup (email captured to Supabase, no sending required)
     · Sitemap.xml and robots.txt for SEO
     · JSON-LD structured data (Product, Review, Article schema)
     · Legal pages: Affiliate Disclosure, Privacy Policy, Terms of Use
     · Cookie consent banner
     · Responsive, mobile-first design

2. TECH ARCHITECTURE
   - Frontend: Next.js App Router with Server Components for all public 
     pages (SSG/ISR for performance and SEO). Client components only for 
     interactive elements like search and the admin form.
   - Backend: Supabase as the database. Next.js Server Actions for the 
     admin form submission. A single API route (/api/webhook/n8n) acts as 
     the webhook receiver from n8n to write new posts.
   - Automation: n8n workflow triggered by admin form submission via webhook.
     n8n calls Claude API to generate content, saves images to Google Drive, 
     then writes the completed post to Supabase via its REST API.
   - Auth: Supabase Auth (email/password). Only admin role can access the 
     dashboard and submission form. Public routes are fully open.
   - Storage: Google Drive (via n8n service account) for product images. 
     Public image URLs stored in Supabase and served via next/image.
   - Deployment: Vercel (frontend + API routes). Supabase hosted project.

3. FOLDER STRUCTURE
   Full Next.js App Router directory tree including:
   app/
     (public)/
       page.tsx                  — Homepage
       blog/page.tsx             — Blog index
       blog/[slug]/page.tsx      — Individual post
       category/[slug]/page.tsx  — Category page
       about/page.tsx
       affiliate-disclosure/page.tsx
       privacy-policy/page.tsx
       terms/page.tsx
     (admin)/
       dashboard/page.tsx        — Admin home
       dashboard/new-review/page.tsx — Review request form
     api/
       webhook/n8n/route.ts      — Receives n8n POST, writes to Supabase
   components/
     ui/                         — shadcn/ui components
     layout/                     — Header, Footer, Sidebar
     review/                     — ReviewCard, ReviewDetail, StarRating, 
                                   ProsConsList, AffiliateButton, RelatedPosts
     blog/                       — PostCard, PostList, CategoryFilter, SearchBar
     common/                     — NewsletterSignup, CookieBanner, 
                                   BreadcrumbNav
   lib/
     supabase/                   — client.ts, server.ts, middleware.ts
     validations.ts              — Zod schemas
     utils.ts
   types/
     index.ts                    — All shared TypeScript types
   hooks/
     useSearch.ts
   actions/
     newsletter.ts               — Server action for email capture
     review-request.ts           — Server action for admin form
   public/
     robots.txt
     sitemap.xml (auto-generated via Next.js sitemap.ts)

4. TYPESCRIPT TYPES
   All shared types in types/index.ts including:
   - Post (id, title, slug, excerpt, body, category, rating, pros, cons, 
     verdict, amazon_url, image_url, published_at, is_published)
   - Category (id, name, slug, description)
   - NewsletterSubscriber (id, email, created_at)
   - ReviewRequest (product_name, category, amazon_url, notes)
   - AdminUser (id, email, role)
   Supabase generated types scaffold

5. DATABASE DESIGN (Supabase)
   Tables:
   - posts (id, title, slug, excerpt, body, category_id, rating, pros[], 
     cons[], verdict, amazon_url, image_url, is_published, 
     published_at, created_at, updated_at)
   - categories (id, name, slug, description, created_at)
   - newsletter_subscribers (id, email, created_at)
   - profiles (id references auth.users, role: 'admin'|'user', 
     full_name, created_at)
   Full SQL CREATE TABLE statements ready to run
   Indexes on: posts.slug, posts.category_id, posts.is_published, 
   posts.published_at
   Foreign keys: posts.category_id → categories.id

6. AUTHENTICATION & ROLES
   - Supabase Auth: email/password only
   - Roles: admin (can access dashboard and submit review requests), 
     public (no auth required for reading)
   - Role stored in profiles.role
   - Admin email set manually in Supabase dashboard after first signup
   - Access control: middleware.ts protects /dashboard/* routes

7. ROW LEVEL SECURITY (RLS)
   - posts: SELECT open to all for is_published = true. INSERT/UPDATE 
     allowed only for service_role (used by n8n webhook).
   - categories: SELECT open to all. INSERT/UPDATE admin only.
   - newsletter_subscribers: INSERT open to all. SELECT admin only.
   - profiles: Users can read/update their own row only.

8. ZOD VALIDATION SCHEMAS (lib/validations.ts)
   - ReviewRequestSchema: product_name, category, amazon_url (valid URL), 
     optional notes
   - NewsletterSchema: email (valid email)
   - WebhookPayloadSchema: validates incoming n8n POST body before writing 
     to Supabase

9. MIDDLEWARE (middleware.ts)
   - Protects /dashboard/* — redirects unauthenticated users to /login
   - Checks profile.role === 'admin' — redirects non-admins to homepage
   - All other routes are public

10. CORE FEATURES — STEP-BY-STEP BUILD
    Module 1: Auth + Admin role setup
    Module 2: Database schema + seed data (3 categories, 2 sample posts)
    Module 3: Public homepage — featured posts grid, latest reviews, 
              category links
    Module 4: Blog index — paginated post list, search, category filter
    Module 5: Individual post page — full review layout with star rating, 
              pros/cons, verdict, Amazon affiliate button, related posts
    Module 6: Category pages
    Module 7: Admin dashboard + review request form + Server Action
    Module 8: n8n webhook API route (/api/webhook/n8n) — validates payload, 
              writes post to Supabase, returns 200
    Module 9: Newsletter signup component + Server Action
    Module 10: SEO — generateMetadata per page, JSON-LD schemas, 
               sitemap.ts, robots.ts
    Module 11: Legal pages (Affiliate Disclosure, Privacy Policy, Terms)
    Module 12: Cookie consent banner

11. UI DESIGN & COMPONENTS
    - Layout: sticky header with logo + category nav + search icon, 
      main content area, sidebar (popular posts, categories, newsletter 
      signup), footer with legal links and affiliate disclosure notice
    - Homepage: hero banner, featured review cards (image, title, rating, 
      excerpt, CTA), category grid, latest posts strip
    - Review page: product image (top), title, star rating, quick verdict 
      box, pros/cons two-column list, full body content, Amazon affiliate 
      button (prominent, orange), related posts at bottom
    - Mobile-first, fully responsive
    - Loading skeleton states for post lists
    - Empty state for search with no results

12. SERVER ACTIONS (actions/ folder)
    - submitReviewRequest() — validates form, logs to Supabase 
      review_requests table, triggers n8n webhook via fetch POST
    - subscribeToNewsletter() — validates email, upserts to 
      newsletter_subscribers
    - Both include revalidatePath after mutations

13. API ROUTES
    - /api/webhook/n8n (POST) — receives completed post payload from n8n, 
      validates with WebhookPayloadSchema, inserts into posts table using 
      Supabase service role key. Protected by a shared secret header 
      (X-Webhook-Secret checked against env variable).

14. ERROR HANDLING
    - Global error.tsx and not-found.tsx
    - Try/catch in all Server Actions with user-facing error messages
    - Webhook route returns structured error responses (400, 401, 500)

15. SEO & METADATA
    - generateMetadata() for every dynamic page (post, category)
    - Static metadata for homepage, blog index, legal pages
    - OpenGraph tags with product image
    - JSON-LD: Product schema on review pages, Article schema on blog posts
    - robots.ts: allow all crawlers, disallow /dashboard
    - sitemap.ts: auto-generates from all published posts and categories

16. IMAGE OPTIMIZATION
    - next/image for all post images
    - Images stored in Google Drive, public URL saved in posts.image_url
    - Fallback placeholder image if image_url is null
    - Add drive.google.com to next.config.js remotePatterns

17. THIRD-PARTY INTEGRATIONS
    - n8n (self-hosted): webhook trigger → Claude API → Google Drive → 
      Supabase REST API
    - Claude API: called inside n8n to generate review content
    - Google Drive: product image storage via n8n service account
    - PostHog: page view and affiliate click event tracking
    - Sentry: error capture on both client and server

18. CACHING STRATEGY
    - Homepage and category pages: ISR with revalidate: 3600 (1 hour)
    - Individual post pages: ISR with revalidate: 86400 (24 hours), 
      revalidated on new post via revalidatePath in webhook route
    - Admin dashboard: dynamic (no cache)
    - Newsletter action: no cache

19. ENVIRONMENT VARIABLES (.env.local)
    NEXT_PUBLIC_SUPABASE_URL=           (public)
    NEXT_PUBLIC_SUPABASE_ANON_KEY=      (public)
    SUPABASE_SERVICE_ROLE_KEY=          (private — used in webhook route)
    WEBHOOK_SECRET=                      (private — shared with n8n)
    NEXT_PUBLIC_POSTHOG_KEY=            (public)
    NEXT_PUBLIC_SITE_URL=               (public — used for OG tags)
    SENTRY_DSN=                         (private)
    NEXT_PUBLIC_AMAZON_TRACKING_ID=     (public — affiliate tag)

20. DEPLOYMENT GUIDE
    - Step-by-step Vercel deployment
    - All env variables added to Vercel dashboard
    - Supabase project URL whitelisted in Vercel
    - n8n webhook URL updated to point to production Vercel URL 
      after deploy
    - Custom domain setup (optional)
    - Post-deploy checklist: test webhook end-to-end, verify affiliate 
      links include tracking ID, confirm sitemap is accessible

21. SAMPLE DATA
    - 3 categories: Kitchen Gadgets, Home Tech, Fitness Gear
    - 2 fully written sample posts with real pros, cons, rating, 
      verdict, and placeholder Amazon URL
    - SQL INSERT statements ready to run in Supabase SQL editor

22. FUTURE IMPROVEMENTS
    - User-submitted reviews with moderation queue
    - Email newsletter sending via Resend
    - Price comparison widget (multiple retailers)
    - YouTube video embed per review
    - Comparison table pages (Product A vs Product B)
    - Stripe-gated premium content tier


Let me pull up the Master Generator Prompt from your document so I can fill it in properly.Got everything I need. Here is your fully filled-in Master Generator Prompt, ready to paste into Cursor:

---

```
You are a senior full-stack engineer.

Build a complete, production-ready web app using:
- Next.js 14+ (App Router)
- TypeScript (strict mode)
- Supabase (Auth, Database, Storage, RLS)
- Tailwind CSS + shadcn/ui
- Zod for validation
- Deployed on Vercel

APP IDEA:
A product review website where AI-generated blog posts and product reviews 
are published automatically via an n8n automation pipeline. Readers browse 
reviews by category, compare products, and click through to Amazon via 
affiliate links. An admin submits a review request through a form, which 
triggers n8n to generate a full blog post using the Claude API, upload any 
images to Google Drive, store all content in Supabase, and publish it to 
the site automatically — no manual publishing required.

GOAL:
Generate a clean, scalable, beginner-friendly project that is fully 
functional. No placeholder logic. All code must be complete and immediately 
runnable.

---

OUTPUT FORMAT (STRICT):

1. PROJECT OVERVIEW
   - Brief description: A content-driven Amazon affiliate review site where 
     all blog posts and product reviews are generated by Claude AI and 
     published automatically via n8n. The admin never writes content manually.
   - Target users: Readers searching for product recommendations and buying 
     guides. Admin manages topic submissions only.
   - Core features (list only what will actually be built):
     · Public homepage with featured reviews and latest posts
     · Category pages (e.g., Kitchen, Tech, Home, Fitness, Beauty)
     · Individual product review pages with pros/cons, star rating, 
       verdict, and Amazon affiliate buy button
     · Blog index page with search and category filter
     · Individual blog post pages with SEO metadata
     · Admin dashboard (protected, role: admin) with a review request form
     · n8n webhook endpoint that receives form submission and triggers 
       AI content generation
     · Claude API integration in n8n generates full review post 
       (title, excerpt, body, pros, cons, rating, verdict, 
       Amazon affiliate link placeholder)
     · Google Drive integration in n8n for product image storage
     · Supabase as the database and content store — n8n writes directly 
       via Supabase REST API
     · Auto-publish on save — no manual approval step needed
     · Related posts widget on each review page
     · Newsletter signup (email captured to Supabase, no sending required)
     · Sitemap.xml and robots.txt for SEO
     · JSON-LD structured data (Product, Review, Article schema)
     · Legal pages: Affiliate Disclosure, Privacy Policy, Terms of Use
     · Cookie consent banner
     · Responsive, mobile-first design

2. TECH ARCHITECTURE
   - Frontend: Next.js App Router with Server Components for all public 
     pages (SSG/ISR for performance and SEO). Client components only for 
     interactive elements like search and the admin form.
   - Backend: Supabase as the database. Next.js Server Actions for the 
     admin form submission. A single API route (/api/webhook/n8n) acts as 
     the webhook receiver from n8n to write new posts.
   - Automation: n8n workflow triggered by admin form submission via webhook.
     n8n calls Claude API to generate content, saves images to Google Drive, 
     then writes the completed post to Supabase via its REST API.
   - Auth: Supabase Auth (email/password). Only admin role can access the 
     dashboard and submission form. Public routes are fully open.
   - Storage: Google Drive (via n8n service account) for product images. 
     Public image URLs stored in Supabase and served via next/image.
   - Deployment: Vercel (frontend + API routes). Supabase hosted project.

3. FOLDER STRUCTURE
   Full Next.js App Router directory tree including:
   app/
     (public)/
       page.tsx                  — Homepage
       blog/page.tsx             — Blog index
       blog/[slug]/page.tsx      — Individual post
       category/[slug]/page.tsx  — Category page
       about/page.tsx
       affiliate-disclosure/page.tsx
       privacy-policy/page.tsx
       terms/page.tsx
     (admin)/
       dashboard/page.tsx        — Admin home
       dashboard/new-review/page.tsx — Review request form
     api/
       webhook/n8n/route.ts      — Receives n8n POST, writes to Supabase
   components/
     ui/                         — shadcn/ui components
     layout/                     — Header, Footer, Sidebar
     review/                     — ReviewCard, ReviewDetail, StarRating, 
                                   ProsConsList, AffiliateButton, RelatedPosts
     blog/                       — PostCard, PostList, CategoryFilter, SearchBar
     common/                     — NewsletterSignup, CookieBanner, 
                                   BreadcrumbNav
   lib/
     supabase/                   — client.ts, server.ts, middleware.ts
     validations.ts              — Zod schemas
     utils.ts
   types/
     index.ts                    — All shared TypeScript types
   hooks/
     useSearch.ts
   actions/
     newsletter.ts               — Server action for email capture
     review-request.ts           — Server action for admin form
   public/
     robots.txt
     sitemap.xml (auto-generated via Next.js sitemap.ts)

4. TYPESCRIPT TYPES
   All shared types in types/index.ts including:
   - Post (id, title, slug, excerpt, body, category, rating, pros, cons, 
     verdict, amazon_url, image_url, published_at, is_published)
   - Category (id, name, slug, description)
   - NewsletterSubscriber (id, email, created_at)
   - ReviewRequest (product_name, category, amazon_url, notes)
   - AdminUser (id, email, role)
   Supabase generated types scaffold

5. DATABASE DESIGN (Supabase)
   Tables:
   - posts (id, title, slug, excerpt, body, category_id, rating, pros[], 
     cons[], verdict, amazon_url, image_url, is_published, 
     published_at, created_at, updated_at)
   - categories (id, name, slug, description, created_at)
   - newsletter_subscribers (id, email, created_at)
   - profiles (id references auth.users, role: 'admin'|'user', 
     full_name, created_at)
   Full SQL CREATE TABLE statements ready to run
   Indexes on: posts.slug, posts.category_id, posts.is_published, 
   posts.published_at
   Foreign keys: posts.category_id → categories.id

6. AUTHENTICATION & ROLES
   - Supabase Auth: email/password only
   - Roles: admin (can access dashboard and submit review requests), 
     public (no auth required for reading)
   - Role stored in profiles.role
   - Admin email set manually in Supabase dashboard after first signup
   - Access control: middleware.ts protects /dashboard/* routes

7. ROW LEVEL SECURITY (RLS)
   - posts: SELECT open to all for is_published = true. INSERT/UPDATE 
     allowed only for service_role (used by n8n webhook).
   - categories: SELECT open to all. INSERT/UPDATE admin only.
   - newsletter_subscribers: INSERT open to all. SELECT admin only.
   - profiles: Users can read/update their own row only.

8. ZOD VALIDATION SCHEMAS (lib/validations.ts)
   - ReviewRequestSchema: product_name, category, amazon_url (valid URL), 
     optional notes
   - NewsletterSchema: email (valid email)
   - WebhookPayloadSchema: validates incoming n8n POST body before writing 
     to Supabase

9. MIDDLEWARE (middleware.ts)
   - Protects /dashboard/* — redirects unauthenticated users to /login
   - Checks profile.role === 'admin' — redirects non-admins to homepage
   - All other routes are public

10. CORE FEATURES — STEP-BY-STEP BUILD
    Module 1: Auth + Admin role setup
    Module 2: Database schema + seed data (3 categories, 2 sample posts)
    Module 3: Public homepage — featured posts grid, latest reviews, 
              category links
    Module 4: Blog index — paginated post list, search, category filter
    Module 5: Individual post page — full review layout with star rating, 
              pros/cons, verdict, Amazon affiliate button, related posts
    Module 6: Category pages
    Module 7: Admin dashboard + review request form + Server Action
    Module 8: n8n webhook API route (/api/webhook/n8n) — validates payload, 
              writes post to Supabase, returns 200
    Module 9: Newsletter signup component + Server Action
    Module 10: SEO — generateMetadata per page, JSON-LD schemas, 
               sitemap.ts, robots.ts
    Module 11: Legal pages (Affiliate Disclosure, Privacy Policy, Terms)
    Module 12: Cookie consent banner

11. UI DESIGN & COMPONENTS
    - Layout: sticky header with logo + category nav + search icon, 
      main content area, sidebar (popular posts, categories, newsletter 
      signup), footer with legal links and affiliate disclosure notice
    - Homepage: hero banner, featured review cards (image, title, rating, 
      excerpt, CTA), category grid, latest posts strip
    - Review page: product image (top), title, star rating, quick verdict 
      box, pros/cons two-column list, full body content, Amazon affiliate 
      button (prominent, orange), related posts at bottom
    - Mobile-first, fully responsive
    - Loading skeleton states for post lists
    - Empty state for search with no results

12. SERVER ACTIONS (actions/ folder)
    - submitReviewRequest() — validates form, logs to Supabase 
      review_requests table, triggers n8n webhook via fetch POST
    - subscribeToNewsletter() — validates email, upserts to 
      newsletter_subscribers
    - Both include revalidatePath after mutations

13. API ROUTES
    - /api/webhook/n8n (POST) — receives completed post payload from n8n, 
      validates with WebhookPayloadSchema, inserts into posts table using 
      Supabase service role key. Protected by a shared secret header 
      (X-Webhook-Secret checked against env variable).

14. ERROR HANDLING
    - Global error.tsx and not-found.tsx
    - Try/catch in all Server Actions with user-facing error messages
    - Webhook route returns structured error responses (400, 401, 500)

15. SEO & METADATA
    - generateMetadata() for every dynamic page (post, category)
    - Static metadata for homepage, blog index, legal pages
    - OpenGraph tags with product image
    - JSON-LD: Product schema on review pages, Article schema on blog posts
    - robots.ts: allow all crawlers, disallow /dashboard
    - sitemap.ts: auto-generates from all published posts and categories

16. IMAGE OPTIMIZATION
    - next/image for all post images
    - Images stored in Google Drive, public URL saved in posts.image_url
    - Fallback placeholder image if image_url is null
    - Add drive.google.com to next.config.js remotePatterns

17. THIRD-PARTY INTEGRATIONS
    - n8n (self-hosted): webhook trigger → Claude API → Google Drive → 
      Supabase REST API
    - Claude API: called inside n8n to generate review content
    - Google Drive: product image storage via n8n service account
    - PostHog: page view and affiliate click event tracking
    - Sentry: error capture on both client and server

18. CACHING STRATEGY
    - Homepage and category pages: ISR with revalidate: 3600 (1 hour)
    - Individual post pages: ISR with revalidate: 86400 (24 hours), 
      revalidated on new post via revalidatePath in webhook route
    - Admin dashboard: dynamic (no cache)
    - Newsletter action: no cache

19. ENVIRONMENT VARIABLES (.env.local)
    NEXT_PUBLIC_SUPABASE_URL=           (public)
    NEXT_PUBLIC_SUPABASE_ANON_KEY=      (public)
    SUPABASE_SERVICE_ROLE_KEY=          (private — used in webhook route)
    WEBHOOK_SECRET=                      (private — shared with n8n)
    NEXT_PUBLIC_POSTHOG_KEY=            (public)
    NEXT_PUBLIC_SITE_URL=               (public — used for OG tags)
    SENTRY_DSN=                         (private)
    NEXT_PUBLIC_AMAZON_TRACKING_ID=     (public — affiliate tag)

20. DEPLOYMENT GUIDE
    - Step-by-step Vercel deployment
    - All env variables added to Vercel dashboard
    - Supabase project URL whitelisted in Vercel
    - n8n webhook URL updated to point to production Vercel URL 
      after deploy
    - Custom domain setup (optional)
    - Post-deploy checklist: test webhook end-to-end, verify affiliate 
      links include tracking ID, confirm sitemap is accessible

21. SAMPLE DATA
    - 3 categories: Kitchen Gadgets, Home Tech, Fitness Gear
    - 2 fully written sample posts with real pros, cons, rating, 
      verdict, and placeholder Amazon URL
    - SQL INSERT statements ready to run in Supabase SQL editor

22. FUTURE IMPROVEMENTS
    - User-submitted reviews with moderation queue
    - Email newsletter sending via Resend
    - Price comparison widget (multiple retailers)
    - YouTube video embed per review
    - Comparison table pages (Product A vs Product B)
    - Stripe-gated premium content tier
```

---

A few things I filled in that you didn't mention but are essential for this type of site: the affiliate disclosure legal page (Amazon requires this for Associates approval), the `NEXT_PUBLIC_AMAZON_TRACKING_ID` env variable so your tag gets appended to every product link, the webhook secret header so random people can't push fake posts to your site, and PostHog affiliate click tracking so you can actually see which posts are driving clicks to Amazon.

When you're ready, drop this into Cursor and run it. Want me to also draft the n8n workflow layout for the automation side?
-------------------------------------------------------------------------
Identity added: C:\Users\louie\.ssh\id_ed25519 (webllrzjohnson@gmail.com)
PS C:\WINDOWS\system32> ssh-add -l
256 SHA256:Pf0aXjzm7UEMlpi/Fl4rJiFk9eV+0pU8UyR3HWPOgOw webllrzjohnson@gmail.com (ED25519)
ssh-keygen -t ed25519 -C "webllrzjohnson@gmail.com"

paraphrase:rypgacwiwJo8vozbak
------------------------------------------------------------------------------


Supabase:

ProjectName: reviewMax
DB Pass: rypgacwiwJo8vozbak

Project URL: https://zcupkkhukceqgajuaxnk.supabase.co
SERVICE ROLE KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpjdXBra2h1a2NlcWdhanVheG5rIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NzgxNjUwOCwiZXhwIjoyMDkzMzkyNTA4fQ._j_IL2D6mIBXpwy3jBLN_TyuOIJTc9YXxewwksAy3W8

Anon Public Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpjdXBra2h1a2NlcWdhanVheG5rIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzc4MTY1MDgsImV4cCI6MjA5MzM5MjUwOH0.zZ8r5NktRQgLQJpRImE0vSfEDqNHfH6XphNBquJoJ6I

User: Shuai.jan28@gmail.com
pw:marlb0r0@@11

UUID: aa97b4ae-c388-4087-bb06-cd7b3d664ddb


-------------------------------------------------------------

AMAZON:

Your unique Associate ID is lesterj-20

---------------------------------------------------------------


$body = @{
  title = "Test Post from n8n"
  slug = "test-post-from-n8n"
  excerpt = "This is a test post."
  body = "<p>Test body content.</p>"
  category_id = "2da59313-402d-4fe9-bbea-a171555bb640"
  rating = 4.5
  pros = @("Easy to use", "Great value")
  cons = @("Limited features")
  verdict = "A solid choice overall."
  amazon_url = "https://www.amazon.com/dp/test"
  image_url = $null
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:3000/api/webhook/n8n" `
  -Method POST `
  -Headers @{
    "Content-Type" = "application/json"
    "X-Webhook-Secret" = "your-webhook-secret-from-env"
  } `
  -Body $body

